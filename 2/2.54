(define (equal? q1 q2)
  (cond ((and (not (pair? q1))
	      (not (pair? q2)))
	 (eq? q1 q2))
	((and (pair? q1)
	      (pair? q2))
	 (and (equal? (car q1) (car q2))
	      (equal? (cdr q1) (cdr q2))))
	(else false)))
